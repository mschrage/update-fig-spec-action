import type { PresetName } from "./presets";
export interface MergeOptions {
    ignore?: {
        commandProps?: string[];
        optionProps?: string[];
        argProps?: string[];
        commonProps?: string[];
    };
    preset?: PresetName;
}
export default function merge(oldFileContent: string, newFileContent: string, options: MergeOptions): string;
