import { SourceFile, TransformerFactory } from "typescript";
/**
 * The TypeScript transformer to rename the export spec variable name to SPEC_NAME,
 * and remove the export keyword.
 *
 * @param context The current context
 */
export declare const specTransformer: TransformerFactory<SourceFile>;
